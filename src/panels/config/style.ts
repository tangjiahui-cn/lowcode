import { css } from 'class-css';

export const page = css({
  display: 'flex',
  alignItems: 'center',
  alignContent: 'flex-start',
  justifyContent: 'space-between',
  padding: '0 8px',
  height: '100%',
});
