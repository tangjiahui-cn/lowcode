import { global } from './global';
import { project } from './project';
import { component } from './component';

export const api = {
  global, // 全局配置Api
  project, // 项目面板Api
  component, // 组件面板Api
};
