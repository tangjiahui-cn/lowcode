export const EVENT = {
  // 设置 max-z-index
  SET_MAX_Z_INDEX: Symbol(),
  // 选中组件
  SELECTED_COMPONENT: Symbol(),
  // 滚动事件
  SCROLL: Symbol(),
  // 修改属性
  SET_ATTRIBUTES: Symbol(),
  // 更新JSON编辑器
  JSON_EDITOR: Symbol()
}
